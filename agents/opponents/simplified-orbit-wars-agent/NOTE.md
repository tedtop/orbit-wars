# Not extractable

This notebook is only a packaging script. It copies `main.py` + `orbit_lite/` from a private Kaggle Models artifact (`/kaggle/input/models/jek1wantaufik/buddy/other/orbit-wars/5/submission`) and tars it. The actual policy is an **orbit_lite-family** bot whose code/weights are not in the notebook, so it cannot be reconstructed locally. Treat as another Producer-lineage agent.
